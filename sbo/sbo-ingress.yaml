apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: sbo-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: [THIS FILE IS DYNAMICALLY GENERATED DURING DEMO CREATION]
      http:
        paths:
          - path: "/"
            backend:
              serviceName: spring-boot-observer-server
              servicePort: 5112
