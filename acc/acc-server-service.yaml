---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/part-of: acc-server
  name: acc-server
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app.kubernetes.io/name: acc-server
  #type: LoadBalancer
